[pytest]
# 自定义 mark 注册，避免 PytestUnknownMarkWarning
markers =
    db: 使用 Mock 替代真实 DB 的单元测试（无需 PostgreSQL）
    integration: 需要真实 PostgreSQL + 已导入数据才能运行的集成测试

# 测试发现路径
testpaths = tests

# 输出格式
addopts = --tb=short
